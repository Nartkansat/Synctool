<Window x:Class="Synctool.Views.CampaignCheckWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Kampanyaları Kontrol Et" 
        Height="700" Width="1100"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- Custom thin scrollbar style -->
        <Style x:Key="SleekScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="rectangle"
                                CornerRadius="3"
                                Background="#CBD5E1"
                                Height="Auto"
                                Width="6"
                                HorizontalAlignment="Center"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="rectangle" Property="Background" Value="#94A3B8"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="true">
                                <Setter TargetName="rectangle" Property="Background" Value="#64748B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SleekScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Margin" Value="2,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true" Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsEnabled}">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource SleekScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SleekScrollViewer" TargetType="{x:Type ScrollViewer}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                    CanContentScroll="{TemplateBinding CanContentScroll}"
                                                    CanHorizontallyScroll="False"
                                                    CanVerticallyScroll="False"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                                    Content="{TemplateBinding Content}"
                                                    Grid.Column="0"
                                                    Margin="{TemplateBinding Padding}"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Style="{StaticResource SleekScrollBar}"
                                       AutomationProperties.AutomationId="VerticalScrollBar"
                                       Cursor="Arrow"
                                       Grid.Column="1"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       Minimum="0"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Implicit Styles for ListBox and DataGrid to use sleek scrollbars -->
        <Style TargetType="ScrollBar" BasedOn="{StaticResource SleekScrollBar}" />
    </Window.Resources>
    
    <Border Background="White" CornerRadius="16" Margin="20">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="30" Opacity="0.2" Direction="270" ShadowDepth="5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#1E293B" CornerRadius="16,16,0,0" Padding="20">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="TicketPercentOutline" Width="28" Height="28" Foreground="#38BDF8" VerticalAlignment="Center" Margin="0,0,12,0"/>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Sepetteki Kampanyalar" FontSize="20" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="Sistemdeki güncel kampanyalar kontrol ediliyor" FontSize="12" Foreground="#94A3B8" Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                            Style="{StaticResource MaterialDesignIconButton}" 
                            Foreground="#94A3B8"
                            Width="36" Height="36"
                            Click="BtnClose_Click" ToolTip="Kapat">
                        <materialDesign:PackIcon Kind="Close" Width="24" Height="24"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Loading Overlay -->
            <Grid x:Name="LoadingOverlay" Grid.Row="1" Panel.ZIndex="10" Background="#CCFFFFFF" Visibility="Visible">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" Value="0" IsIndeterminate="True" Width="50" Height="50" Foreground="#4F46E5"/>
                    <TextBlock Text="Kampanyalar Taranıyor..." FontSize="16" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,16,0,0" HorizontalAlignment="Center"/>
                    <TextBlock Text="Bu işlem birkaç saniye sürebilir" FontSize="12" Foreground="#64748B" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>

            <!-- Main Content -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="320"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sol Panel: Sepetteki Ürünler -->
                <Border Grid.Column="0" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="0,0,1,0" CornerRadius="0,0,0,16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <Border Grid.Row="0" Padding="16,12" Background="#F1F5F9" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1">
                            <TextBlock Text="Sepetteki Ürünler" FontSize="14" FontWeight="Bold" Foreground="#475569"/>
                        </Border>

                        <ListBox x:Name="ListCartItems" Grid.Row="1" Background="Transparent" BorderThickness="0" 
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 materialDesign:ScrollViewerAssist.IsAutoHideEnabled="True"
                                 SelectionChanged="ListCartItems_SelectionChanged"
                                 Style="{StaticResource MaterialDesignListBox}"
                                 ItemContainerStyle="{StaticResource MaterialDesignListBoxItem}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="12" Margin="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <Border Width="40" Height="40" CornerRadius="8" Background="#EEF2FF" VerticalAlignment="Center">
                                                <materialDesign:PackIcon Kind="PackageVariantClosed" Foreground="#4F46E5" Width="20" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            
                                            <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding ProductName}" FontWeight="Bold" FontSize="13" Foreground="#1E293B" TextWrapping="Wrap" MaxHeight="36" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding ProductCode}" FontSize="11" Foreground="#64748B" Margin="0,4,0,0" FontFamily="Consolas"/>
                                            </StackPanel>

                                            <!-- Badge for Campaign Count -->
                                            <Border Grid.Column="2" Background="{Binding CampaignCountColor}" CornerRadius="12" Padding="8,4" VerticalAlignment="Center" Margin="8,0,0,0">
                                                <TextBlock Text="{Binding CampaignCountText}" FontSize="11" FontWeight="Bold" Foreground="White"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <!-- Sağ Panel: Seçili Ürünün Kampanyaları -->
                <Grid Grid.Column="1" Background="White">
                    
                    <StackPanel x:Name="PanelNoSelection" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                        <materialDesign:PackIcon Kind="HandPointingLeft" Width="64" Height="64" Foreground="#CBD5E1" HorizontalAlignment="Center"/>
                        <TextBlock Text="Kampanyalarını görmek için soldan bir ürün seçin" FontSize="16" FontWeight="SemiBold" Foreground="#94A3B8" Margin="0,16,0,0"/>
                    </StackPanel>

                    <Grid x:Name="PanelCampaigns" Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Ürün Başlığı -->
                        <Border Grid.Row="0" Padding="24" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1">
                            <StackPanel>
                                <TextBlock Text="Kampanyalar" FontSize="18" FontWeight="ExtraBold" Foreground="#1E293B"/>
                                <TextBlock x:Name="TxtSelectedProductName" Text="Ürün Adı" FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" 
                                      Style="{StaticResource SleekScrollViewer}"
                                      materialDesign:ScrollViewerAssist.IsAutoHideEnabled="True">
                            <StackPanel>
                                <ItemsControl x:Name="ItemsCampaigns" Margin="24">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,0,16">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>

                                                    <Grid Grid.Row="0" Margin="0,0,0,12">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel>
                                                            <TextBlock Text="{Binding CampaignCode}" FontSize="12" FontWeight="Bold" Foreground="#4F46E5" FontFamily="Consolas"/>
                                                            <TextBlock Text="{Binding ShortDescription}" FontSize="16" FontWeight="ExtraBold" Foreground="#1E293B" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                                        </StackPanel>
                                                        <Border Grid.Column="1" Background="#ECFDF5" CornerRadius="8" Padding="12,6" VerticalAlignment="Top">
                                                            <StackPanel>
                                                                <TextBlock Text="İndirim" FontSize="10" Foreground="#059669" HorizontalAlignment="Center"/>
                                                                <TextBlock Text="{Binding DiscountAmount, StringFormat={}{0:N2} ₺}" FontSize="16" FontWeight="Black" Foreground="#059669"/>
                                                            </StackPanel>
                                                        </Border>
                                                    </Grid>

                                                    <TextBlock Grid.Row="1" Text="{Binding GeneralDescription}" FontSize="13" Foreground="#475569" TextWrapping="Wrap" Margin="0,0,0,16"/>

                                                    <!-- Dates -->
                                                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,16" Visibility="Collapsed">
                                                        <materialDesign:PackIcon Kind="CalendarClock" Width="16" Height="16" Foreground="#94A3B8" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding StartDate}" FontSize="12" Foreground="#64748B" Margin="6,0,0,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="-" FontSize="12" Foreground="#64748B" Margin="4,0" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding EndDate}" FontSize="12" Foreground="#64748B" VerticalAlignment="Center"/>
                                                    </StackPanel>

                                                    <!-- Button: Ürünleri Gör -->
                                                    <Button Grid.Row="3" HorizontalAlignment="Right" 
                                                            Style="{StaticResource MaterialDesignOutlinedButton}" 
                                                            BorderBrush="#4F46E5" Foreground="#4F46E5"
                                                            Height="36" materialDesign:ButtonAssist.CornerRadius="8"
                                                            Click="BtnViewProducts_Click" Tag="{Binding CampaignId}"
                                                            Visibility="{Binding HasTargetProducts, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                        <StackPanel Orientation="Horizontal">
                                                            <materialDesign:PackIcon Kind="FormatListBulleted" Width="18" Height="18" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                            <TextBlock Text="Dahil Olan Ürünleri Gör" FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
                                                        </StackPanel>
                                                    </Button>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <!-- Dahil Olan Ürünler Paneli (Sağdan kayarak gelecek veya overlay olacak) -->
                    <Grid x:Name="PanelLinkedProducts" Visibility="Collapsed" Background="White">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Padding="20" Background="#EEF2FF" BorderBrush="#E0E7FF" BorderThickness="0,0,0,1">
                            <Grid>
                                <StackPanel Orientation="Horizontal">
                                    <Button Style="{StaticResource MaterialDesignIconButton}" Width="32" Height="32" Foreground="#4F46E5" Click="BtnBackToCampaigns_Click" Margin="0,0,12,0">
                                        <materialDesign:PackIcon Kind="ArrowLeft" Width="20" Height="20"/>
                                    </Button>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="Kampanyaya Dahil Ürünler" FontSize="16" FontWeight="Bold" Foreground="#1E293B"/>
                                        <TextBlock x:Name="TxtLinkedSheetName" Text="Liste taranıyor..." FontSize="12" Foreground="#64748B" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Grid x:Name="LinkedProductsLoadingOverlay" Grid.Row="1" Panel.ZIndex="5" Background="#CCFFFFFF" Visibility="Collapsed">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" Value="0" IsIndeterminate="True" Width="40" Height="40" Foreground="#4F46E5"/>
                                <TextBlock Text="Ürünler Çekiliyor..." FontSize="14" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,16,0,0" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>

                        <DataGrid x:Name="GridLinkedProducts" Grid.Row="1" Margin="20" 
                                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                                  HeadersVisibility="Column" Background="White"
                                  Style="{StaticResource MaterialDesignDataGrid}"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  materialDesign:ScrollViewerAssist.IsAutoHideEnabled="True"
                                  materialDesign:DataGridAssist.ColumnHeaderPadding="12,8">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Kolon 1 (Kod)" Binding="{Binding Column1}" Width="150" />
                                <DataGridTextColumn Header="Kolon 2 (Açıklama)" Binding="{Binding Column2}" Width="*" />
                                <DataGridTextColumn Header="Kolon 3 (Ek Bilgi)" Binding="{Binding Column3}" Width="150" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>

                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
